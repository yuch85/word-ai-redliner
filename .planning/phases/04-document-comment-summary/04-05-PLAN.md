---
phase: 04-document-comment-summary
plan: 05
type: execute
wave: 4
depends_on: [04-04]
files_modified:
  - src/lib/comment-extractor.js
  - tests/comment-extractor.spec.js
  - src/taskpane/taskpane.html
  - src/taskpane/taskpane.js
  - src/taskpane/taskpane.css
  - src/lib/prompt-manager.js
  - tests/prompt-composition.spec.js
autonomous: true
requirements: [SUMM-05]

must_haves:
  truths:
    - "extractTrackedChanges returns an array of tracked change objects parsed from OOXML via body.getOoxml() and browser DOMParser"
    - "OOXML parsing handles pkg:package wrapper by extracting the document.xml part before parsing revision marks"
    - "w:proofErr elements are removed during normalization before tracked change extraction"
    - "w:ins elements produce {type: 'Added', text, author, date, paragraphText} objects"
    - "w:del elements produce {type: 'Deleted', text, author, date, paragraphText} objects"
    - "Adjacent w:del + w:ins from the same author (DOM siblings) are paired as {type: 'Replaced', beforeText, afterText}"
    - "w:moveFrom and w:moveTo elements produce {type: 'Moved (from)'} and {type: 'Moved (to)'} objects"
    - "w:ins and w:del inside w:trPr (table row revision markers) are skipped"
    - "Text extraction from runs handles w:t, w:delText, w:br, w:tab, w:cr, and w:noBreakHyphen"
    - "Paragraph context is extracted for each change by walking to parent w:p and concatenating w:t nodes (excluding w:del and w:moveFrom containers)"
    - "Namespace-aware querying uses getElementsByTagNameNS with getElementsByTagName('w:*') fallback"
    - "Author identity is prominently included in the LLM-formatted output for every change"
    - "LLM receives each tracked change with before/after text clearly labeled (BEFORE/AFTER, DELETED/ADDED, IN CLAUSE)"
    - "Settings UI has a toggle to enable/disable tracked changes extraction"
    - "When enabled, tracked changes data is included in the summary prompt via {tracked changes} placeholder"
    - "composeSummaryMessages handles {tracked changes} placeholder replacement"
    - "Word API version is logged on initialization for diagnostics"
  artifacts:
    - path: "src/lib/comment-extractor.js"
      provides: "extractTrackedChanges function using OOXML-only parsing via body.getOoxml() + DOMParser"
      exports: ["extractTrackedChanges"]
    - path: "tests/comment-extractor.spec.js"
      provides: "Tests for extractTrackedChanges covering OOXML parsing, pkg:package handling, pairing, moves, table row skip, w:proofErr normalization"
      contains: "extractTrackedChanges"
    - path: "src/lib/prompt-manager.js"
      provides: "composeSummaryMessages handles {tracked changes} placeholder"
      contains: "tracked changes"
    - path: "tests/prompt-composition.spec.js"
      provides: "Tests for {tracked changes} placeholder replacement in composeSummaryMessages"
      contains: "tracked changes"
    - path: "src/taskpane/taskpane.html"
      provides: "Tracked changes toggle in Settings"
      contains: "trackedChangesExtraction"
    - path: "src/taskpane/taskpane.js"
      provides: "Settings persistence for tracked changes toggle, WordApi version logging, integration with summary workflow"
      contains: "extractTrackedChanges"
  key_links:
    - from: "src/taskpane/taskpane.js"
      to: "src/lib/comment-extractor.js"
      via: "import extractTrackedChanges"
      pattern: "extractTrackedChanges"
    - from: "src/lib/comment-extractor.js"
      to: "Word.run"
      via: "body.getOoxml() for OOXML extraction"
      pattern: "getOoxml"
    - from: "src/lib/prompt-manager.js"
      to: "composeSummaryMessages"
      via: "{tracked changes} placeholder replacement in options.trackedChangesText"
      pattern: "tracked changes"
    - from: "src/taskpane/taskpane.js"
      to: "handleSummaryGeneration"
      via: "extracts tracked changes when toggle is enabled and passes to composeSummaryMessages"
      pattern: "trackedChangesExtraction"
---

<objective>
Add tracked changes extraction to the document extraction pipeline using OOXML-only parsing via body.getOoxml() + browser DOMParser.

Purpose: Tracked changes are a critical part of document review workflows. Including them in the summary prompt gives the LLM visibility into what has been modified, by whom, and when -- producing far more useful document analysis. The OOXML approach is superior to higher-level Word JS API alternatives because it provides: (1) before AND after text for replacements via paired w:del + w:ins, (2) move detection via w:moveFrom/w:moveTo, (3) author + date on every change, (4) paragraph context, and (5) works on ALL Office versions (WordApi 1.1 minimum).

Output: New `extractTrackedChanges()` function with OOXML parsing, Settings UI toggle, `{tracked changes}` placeholder support in summary prompts, and integration with the summary generation workflow.

Key design decision (user locked): Single-tier OOXML parsing only. No cascading API version detection. No Tier 1 (TrackedChange API) or Tier 2 (getReviewedText). This simplifies the codebase and provides richer data than either alternative.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-document-comment-summary/04-CONTEXT.md
@.planning/phases/04-document-comment-summary/04-05-RESEARCH.md

@src/lib/comment-extractor.js
@src/lib/prompt-manager.js
@src/taskpane/taskpane.js
@src/taskpane/taskpane.html
@src/taskpane/taskpane.css
@tests/comment-extractor.spec.js
@tests/prompt-composition.spec.js
</context>

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From src/lib/comment-extractor.js (current, after plan 04-04):
```javascript
const MAX_ASSOCIATED_TEXT_LENGTH = 500;
const MAX_DOCUMENT_TEXT_LENGTH = 50000;

export async function extractAllComments()
// Returns Array<{index, commentText, associatedText, author, date, resolved, id}>

export async function extractDocumentText()
// Returns string (body.text truncated to MAX_DOCUMENT_TEXT_LENGTH)

// Added by plan 04-04:
export async function extractDocumentStructured(options)
// Returns string with richness levels (plain, headings, structured)

export function estimateTokenCount(text)
// Returns Math.ceil(text.length / 4)
```

From src/lib/prompt-manager.js (current composeSummaryMessages):
```javascript
composeSummaryMessages(extractedComments, options = {}) {
    // options.documentText -> replaces {whole document} placeholder
    // Returns [{role, content}]
}
```

From src/taskpane/taskpane.js (config pattern, after plan 04-04):
```javascript
let config = {
    backend: 'ollama',
    trackChangesEnabled: true,
    lineDiffEnabled: false,
    docExtraction: {            // Added by plan 04-04
        richness: 'plain',
        maxLength: 50000
    },
    backends: { ollama: {...}, vllm: {...} }
};
```

From src/taskpane/taskpane.js (existing WordApi detection pattern):
```javascript
// In initialize():
if (typeof Office !== 'undefined' && Office.context && Office.context.requirements) {
    supportsComments = Office.context.requirements.isSetSupported('WordApi', '1.4');
}
```

From src/taskpane/taskpane.js (handleSummaryGeneration, after plan 04-04):
```javascript
// 2. Extract document text if summary prompt uses {whole document} placeholder
const summaryOpts = {};
const activeSummaryPrompt = promptManager.getActivePrompt('summary');
if (activeSummaryPrompt && activeSummaryPrompt.template.includes('{whole document}')) {
    const extraction = config.docExtraction || {};
    const richness = extraction.richness || 'plain';
    const maxLength = extraction.maxLength || 50000;
    addLog(`Extracting document text (${richness}, max ${maxLength} chars)...`, 'info');
    summaryOpts.documentText = await extractDocumentStructured({ richness, maxLength });
}
```

OOXML tracked changes format (for OOXML parsing):
```xml
<!-- body.getOoxml() returns content wrapped in pkg:package -->
<pkg:package xmlns:pkg="http://schemas.microsoft.com/office/2006/xmlPackage">
  <pkg:part pkg:name="/word/document.xml" ...>
    <pkg:xmlData>
      <w:document xmlns:w="http://schemas.openxmlformats.org/wordprocessingml/2006/main">
        <w:body>
          <!-- Actual document content with tracked changes -->
        </w:body>
      </w:document>
    </pkg:xmlData>
  </pkg:part>
</pkg:package>

<!-- Insertion -->
<w:ins w:id="1" w:author="John" w:date="2026-03-10T10:00:00Z">
  <w:r><w:t>inserted text</w:t></w:r>
</w:ins>

<!-- Deletion -->
<w:del w:id="2" w:author="Jane" w:date="2026-03-10T11:00:00Z">
  <w:r><w:delText>deleted text</w:delText></w:r>
</w:del>

<!-- Replacement (adjacent del + ins from same author) -->
<w:del w:id="3" w:author="John" w:date="2026-03-10T12:00:00Z">
  <w:r><w:delText>old text</w:delText></w:r>
</w:del>
<w:ins w:id="4" w:author="John" w:date="2026-03-10T12:00:00Z">
  <w:r><w:t>new text</w:t></w:r>
</w:ins>

<!-- Move operations -->
<w:moveFrom w:id="5" w:author="Jane" w:date="2026-03-10T13:00:00Z">
  <w:r><w:delText>moved text</w:delText></w:r>
</w:moveFrom>
<w:moveTo w:id="6" w:author="Jane" w:date="2026-03-10T13:00:00Z">
  <w:r><w:t>moved text</w:t></w:r>
</w:moveTo>

<!-- Table row revision markers (skip these) -->
<w:tr>
  <w:trPr>
    <w:del w:id="301" w:author="Alice" w:date="2026-01-01T00:00:00Z"/>
  </w:trPr>
  <w:tc><w:p><w:r><w:t>DeleteRow</w:t></w:r></w:p></w:tc>
</w:tr>

<!-- Proofing errors (strip during normalization) -->
<w:proofErr w:type="spellStart"/>
```
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add extractTrackedChanges function with OOXML-only parsing and tests</name>
  <files>src/lib/comment-extractor.js, tests/comment-extractor.spec.js, src/lib/prompt-manager.js, tests/prompt-composition.spec.js</files>
  <behavior>
    - extractTrackedChanges() returns { changes: Array } where each change has {type, text, author, date, paragraphText}
    - Uses body.getOoxml() exclusively (no cascading API detection, no Tier 1 or Tier 2)
    - Handles pkg:package wrapper: extracts /word/document.xml part content before parsing
    - Removes w:proofErr elements during normalization before extracting changes
    - Extracts w:ins elements: { type: 'Added', text: (concatenated run text), author, date, paragraphText }
    - Extracts w:del elements: { type: 'Deleted', text: (concatenated w:delText), author, date, paragraphText }
    - Pairs adjacent w:del + w:ins (same author, DOM siblings) as { type: 'Replaced', beforeText, afterText, author, date, paragraphText }
    - Extracts w:moveFrom: { type: 'Moved (from)', text, author, date, paragraphText }
    - Extracts w:moveTo: { type: 'Moved (to)', text, author, date, paragraphText }
    - Skips w:ins/w:del inside w:trPr (table row revision markers)
    - Text extraction from runs handles: w:t, w:delText, w:br -> \n, w:tab -> \t, w:cr -> \n, w:noBreakHyphen -> \u2011
    - Paragraph context: walks to parent w:p, concatenates w:t excluding w:del and w:moveFrom containers
    - Namespace-aware querying: getElementsByTagNameNS first, getElementsByTagName('w:*') fallback
    - Returns { changes: [] } if extraction fails entirely (logged but not thrown)
    - composeSummaryMessages handles {tracked changes} placeholder in options.trackedChangesText
    - If template has {tracked changes} and options.trackedChangesText is provided, replace all occurrences
    - If template has no {tracked changes} placeholder, tracked changes text is NOT auto-appended
  </behavior>
  <action>
    1. Write tests FIRST in tests/comment-extractor.spec.js (RED phase):
       - Add a new describe block: 'extractTrackedChanges'
       - Important mock note: For OOXML tests, jsdom provides DOMParser. Create test OOXML strings
         with the wordprocessingml namespace. Use the existing Word.run mock pattern from the file.

       **pkg:package wrapper tests:**
         - Test: when OOXML is wrapped in pkg:package, extracts document.xml part and parses tracked changes from it
         - Test: when OOXML has no pkg:package wrapper (raw document XML), parses directly
         - Provide a helper function `wrapInPkgPackage(bodyXml)` that creates a full pkg:package envelope:
           ```javascript
           function wrapInPkgPackage(bodyXml) {
               return `<?xml version="1.0" encoding="UTF-8" standalone="yes"?>` +
                   `<pkg:package xmlns:pkg="http://schemas.microsoft.com/office/2006/xmlPackage">` +
                   `<pkg:part pkg:name="/word/document.xml" pkg:contentType="application/vnd.openxmlformats-officedocument.wordprocessingml.document.main+xml">` +
                   `<pkg:xmlData>` +
                   `<w:document xmlns:w="http://schemas.openxmlformats.org/wordprocessingml/2006/main">` +
                   `<w:body>${bodyXml}</w:body>` +
                   `</w:document>` +
                   `</pkg:xmlData>` +
                   `</pkg:part>` +
                   `</pkg:package>`;
           }
           ```
         - Also provide a helper `makeRawOoxml(bodyXml)` for non-wrapped XML:
           ```javascript
           function makeRawOoxml(bodyXml) {
               return `<w:document xmlns:w="http://schemas.openxmlformats.org/wordprocessingml/2006/main">` +
                   `<w:body>${bodyXml}</w:body></w:document>`;
           }
           ```

       **w:proofErr normalization test:**
         - Test: w:proofErr elements are stripped before change extraction (proofErr inside a paragraph does not break text extraction)

       **Insertion tests:**
         - Test: parses w:ins elements and returns { type: 'Added', text, author, date, paragraphText }
         - Test: handles multiple w:r > w:t elements within a single w:ins (concatenates text)

       **Deletion tests:**
         - Test: parses w:del elements and returns { type: 'Deleted', text (from w:delText), author, date, paragraphText }

       **Replacement pairing tests:**
         - Test: pairs adjacent w:del + w:ins (same author, DOM siblings) as { type: 'Replaced', beforeText, afterText }
         - Test: does NOT pair w:del + w:ins from different authors (creates separate Deleted + Added entries)
         - Test: does NOT pair w:del + w:ins in different paragraphs

       **Move operation tests:**
         - Test: w:moveFrom returns { type: 'Moved (from)', text, author, date }
         - Test: w:moveTo returns { type: 'Moved (to)', text, author, date }

       **Table row marker tests:**
         - Test: skips w:ins/w:del inside w:trPr (table row markers)

       **Text extraction tests:**
         - Test: handles w:br and w:tab in run text extraction (w:br -> \n, w:tab -> \t)
         - Test: handles w:cr as newline and w:noBreakHyphen as \u2011

       **Paragraph context tests:**
         - Test: extracts paragraph context text (excludes deleted text from paragraph)
         - Test: paragraph context includes inserted text (w:t inside w:ins is included)

       **Namespace fallback tests:**
         - Test: uses namespace fallback (getElementsByTagName('w:ins')) when NS query returns empty

       **Edge case tests:**
         - Test: handles OOXML with no tracked changes (returns empty array)
         - Test: returns { changes: [] } when extraction fails (e.g., invalid XML)
         - Test: skips changes with empty/whitespace-only text

       - Run: `npx jest tests/comment-extractor.spec.js -x` -- new tests should FAIL

    2. Write tests in tests/prompt-composition.spec.js for {tracked changes} placeholder:
       - Add a new describe block: 'composeSummaryMessages: {tracked changes} placeholder':
         - Test: replaces {tracked changes} placeholder with options.trackedChangesText when both are present
         - Test: does NOT append tracked changes text when template lacks {tracked changes} placeholder
         - Test: handles template with both {comments}, {whole document}, and {tracked changes} placeholders simultaneously
         - Test: replaces multiple {tracked changes} occurrences
       - Run: `npx jest tests/prompt-composition.spec.js -x` -- new tests should FAIL

    3. Implement extractTrackedChanges in src/lib/comment-extractor.js (GREEN phase):

       Add OOXML parsing helpers (not exported, above extractTrackedChanges).
       These patterns are informed by the docx-redline-js and adeu reference libraries.

       ```javascript
       const W_NS = 'http://schemas.openxmlformats.org/wordprocessingml/2006/main';
       const PKG_NS = 'http://schemas.microsoft.com/office/2006/xmlPackage';

       /**
        * Query OOXML elements with namespace-aware fallback.
        * Tries getElementsByTagNameNS first, falls back to prefix-based getElementsByTagName.
        * Pattern from docx-redline-js: browser DOMParser namespace resolution is inconsistent.
        */
       function queryElements(parent, localName) {
           let elements = parent.getElementsByTagNameNS(W_NS, localName);
           if (elements.length > 0) return Array.from(elements);
           elements = parent.getElementsByTagName('w:' + localName);
           return Array.from(elements);
       }

       /**
        * Extracts the document.xml content from a pkg:package wrapper.
        * body.getOoxml() returns OOXML wrapped in pkg:package; we need the inner document XML.
        * Pattern from adeu: the pkg:part with name="/word/document.xml" contains the actual content.
        *
        * @param {Document} doc - Parsed XML document
        * @returns {Element} - The w:body element or the document element if no wrapper found
        */
       function extractDocumentBody(doc) {
           // Try pkg:package wrapper first
           let xmlDataElements = doc.getElementsByTagNameNS(PKG_NS, 'xmlData');
           if (xmlDataElements.length === 0) {
               xmlDataElements = doc.getElementsByTagName('pkg:xmlData');
           }

           if (xmlDataElements.length > 0) {
               // Find the xmlData containing w:document (could be multiple pkg:parts)
               for (const xmlData of Array.from(xmlDataElements)) {
                   const bodies = queryElements(xmlData, 'body');
                   if (bodies.length > 0) return bodies[0];
               }
           }

           // No pkg:package wrapper -- try direct w:body
           const bodies = queryElements(doc, 'body');
           if (bodies.length > 0) return bodies[0];

           // Fallback to document element
           return doc.documentElement;
       }

       /**
        * Removes w:proofErr elements from the DOM before processing.
        * Pattern from adeu: proofing error markers can interfere with text extraction.
        */
       function removeProofErrors(parent) {
           const proofErrors = queryElements(parent, 'proofErr');
           for (const el of proofErrors) {
               el.parentNode.removeChild(el);
           }
       }

       function getChangeAuthor(element) {
           return element.getAttributeNS(W_NS, 'author')
               || element.getAttribute('w:author')
               || element.getAttribute('author')
               || null;
       }

       function getChangeDate(element) {
           return element.getAttributeNS(W_NS, 'date')
               || element.getAttribute('w:date')
               || element.getAttribute('date')
               || null;
       }

       /**
        * Extract text from a run, handling w:t, w:delText, w:br, w:tab, w:cr, w:noBreakHyphen.
        * Pattern from docx-redline-js ingestion-export.js readRunText().
        */
       function readRunText(run, useDelText = false) {
           let text = '';
           for (const child of Array.from(run.childNodes || [])) {
               if (child.nodeType !== 1) continue;
               const name = child.localName;
               if (name === 't' && !useDelText) text += child.textContent || '';
               else if (name === 'delText' && useDelText) text += child.textContent || '';
               else if (name === 'tab') text += '\t';
               else if (name === 'br' || name === 'cr') text += '\n';
               else if (name === 'noBreakHyphen') text += '\u2011';
           }
           return text;
       }

       /** Extract text from all runs within a revision element. */
       function extractRevisionText(element, useDelText = false) {
           const runs = queryElements(element, 'r');
           let text = '';
           for (const run of runs) text += readRunText(run, useDelText);
           // Fallback: if no runs found but delText exists directly
           if (!text && useDelText) {
               for (const dt of queryElements(element, 'delText')) text += dt.textContent || '';
           }
           return text;
       }

       /**
        * Get containing paragraph's current text for clause context.
        * Skips runs inside w:del or w:moveFrom containers.
        * Pattern from docx-redline-js ingestion-export.js.
        */
       function getContainingParagraphText(changeElement) {
           let node = changeElement;
           while (node && node.localName !== 'p') node = node.parentNode;
           if (!node) return '';
           const textNodes = queryElements(node, 't');
           let text = '';
           for (const t of textNodes) {
               let parent = t.parentNode, excluded = false;
               while (parent && parent !== node) {
                   if (parent.localName === 'del' || parent.localName === 'moveFrom') { excluded = true; break; }
                   parent = parent.parentNode;
               }
               if (!excluded) text += t.textContent || '';
           }
           return text.trim();
       }

       function getNextElementSibling(element) {
           let sibling = element.nextSibling;
           while (sibling && sibling.nodeType !== 1) sibling = sibling.nextSibling;
           return sibling;
       }

       /** Skip w:ins/w:del inside w:trPr (table row property markers, not content). */
       function isTableRowRevisionMarker(element) {
           const parent = element.parentNode;
           return parent && parent.localName === 'trPr';
       }

       /**
        * Parses OOXML for tracked changes using browser DOMParser.
        * Handles: pkg:package wrapper, w:proofErr normalization,
        * w:ins, w:del, w:moveFrom, w:moveTo.
        * Pairs adjacent w:del + w:ins (by DOM sibling, same author) as replacements.
        * Extracts containing paragraph text for clause context.
        * Patterns informed by docx-redline-js and adeu reference libraries.
        */
       function parseOoxmlTrackedChanges(ooxml) {
           const changes = [];
           try {
               const parser = new DOMParser();
               const doc = parser.parseFromString(ooxml, 'application/xml');

               // Extract document body from pkg:package wrapper (if present)
               const body = extractDocumentBody(doc);

               // Normalize: remove w:proofErr elements before processing
               removeProofErrors(body);

               // Process deletions first, pairing with adjacent insertions via DOM siblings
               const deletions = queryElements(body, 'del');
               const pairedInsertions = new Set();

               for (const del of deletions) {
                   if (isTableRowRevisionMarker(del)) continue;
                   const author = getChangeAuthor(del);
                   const date = getChangeDate(del);
                   const delText = extractRevisionText(del, true);
                   if (!delText.trim()) continue;
                   const paragraphText = getContainingParagraphText(del);

                   // DOM-based pairing: check if next element sibling is w:ins from same author
                   const nextSibling = getNextElementSibling(del);
                   if (nextSibling && nextSibling.localName === 'ins' &&
                       getChangeAuthor(nextSibling) === author) {
                       const insText = extractRevisionText(nextSibling, false);
                       if (insText.trim()) {
                           pairedInsertions.add(nextSibling);
                           changes.push({
                               type: 'Replaced', beforeText: delText, afterText: insText,
                               text: insText, author, date, paragraphText
                           });
                           continue;
                       }
                   }
                   changes.push({ type: 'Deleted', text: delText, author, date, paragraphText });
               }

               // Process unpaired insertions
               for (const ins of queryElements(body, 'ins')) {
                   if (pairedInsertions.has(ins) || isTableRowRevisionMarker(ins)) continue;
                   const insText = extractRevisionText(ins, false);
                   if (!insText.trim()) continue;
                   changes.push({
                       type: 'Added', text: insText, author: getChangeAuthor(ins),
                       date: getChangeDate(ins), paragraphText: getContainingParagraphText(ins)
                   });
               }

               // Process move operations
               for (const mf of queryElements(body, 'moveFrom')) {
                   const text = extractRevisionText(mf, true) || extractRevisionText(mf, false);
                   if (!text.trim()) continue;
                   changes.push({
                       type: 'Moved (from)', text, author: getChangeAuthor(mf),
                       date: getChangeDate(mf), paragraphText: getContainingParagraphText(mf)
                   });
               }
               for (const mt of queryElements(body, 'moveTo')) {
                   const text = extractRevisionText(mt, false);
                   if (!text.trim()) continue;
                   changes.push({
                       type: 'Moved (to)', text, author: getChangeAuthor(mt),
                       date: getChangeDate(mt), paragraphText: getContainingParagraphText(mt)
                   });
               }
           } catch (e) {
               console.error('Failed to parse OOXML for tracked changes:', e);
           }
           return changes;
       }
       ```

       Add the main function (exported):
       ```javascript
       /**
        * Extracts tracked changes from the active document using OOXML parsing.
        *
        * Uses body.getOoxml() to get the document's OOXML representation, then
        * parses it with the browser's DOMParser to extract revision marks:
        * w:ins, w:del, w:moveFrom, w:moveTo.
        *
        * This approach works on ALL Office versions (WordApi 1.1 minimum) and
        * provides richer data than higher-level Word JS API alternatives:
        * - Before AND after text for replacements (paired w:del + w:ins)
        * - Move detection (w:moveFrom / w:moveTo)
        * - Author + date on every change
        * - Paragraph context
        *
        * Handles the pkg:package wrapper that body.getOoxml() returns.
        * Normalizes by removing w:proofErr elements before extraction.
        *
        * @returns {Promise<{changes: Array<{type: string, text: string, author: string|null, date: string|null, paragraphText: string}>}>}
        */
       export async function extractTrackedChanges() {
           try {
               let ooxml = '';
               await Word.run(async (context) => {
                   const body = context.document.body;
                   const result = body.getOoxml();
                   await context.sync();
                   ooxml = result.value;
               });
               const changes = parseOoxmlTrackedChanges(ooxml);
               return { changes };
           } catch (e) {
               console.error('Failed to extract tracked changes:', e);
               return { changes: [] };
           }
       }
       ```

    4. Update composeSummaryMessages in src/lib/prompt-manager.js to handle {tracked changes} placeholder:
       - Add `options.trackedChangesText` handling after the existing `{whole document}` replacement block:
         ```javascript
         // Replace {tracked changes} placeholder
         if (content.includes('{tracked changes}') && options.trackedChangesText !== undefined) {
             content = content.replace(/{tracked changes}/g, options.trackedChangesText);
         }
         ```
       - This goes right after the `{whole document}` replacement block in composeSummaryMessages.

    5. Run tests: `npx jest tests/comment-extractor.spec.js tests/prompt-composition.spec.js -x` -- all should PASS
    6. Run full suite: `npx jest --no-coverage -x` -- no regressions

    **Important mock notes for tests:**
    - For OOXML tests: jsdom provides DOMParser natively in the test environment
    - Use the `wrapInPkgPackage(bodyXml)` and `makeRawOoxml(bodyXml)` helpers to construct test OOXML strings
    - The Office global mock is NOT needed for extractTrackedChanges tests since it does not use API detection -- only Word.run is needed
    - Use the existing Word.run mock pattern from the file (global.Word = { run: jest.fn(...) })
    - Set up the mock to return OOXML strings via body.getOoxml():
      ```javascript
      mockContext = {
          document: {
              body: {
                  getOoxml: jest.fn(() => ({ value: testOoxml }))
              }
          },
          sync: jest.fn(async () => {})
      };
      global.Word = {
          run: jest.fn(async (callback) => callback(mockContext))
      };
      ```
  </action>
  <verify>
    <automated>npx jest tests/comment-extractor.spec.js tests/prompt-composition.spec.js --no-coverage -x</automated>
  </verify>
  <done>extractTrackedChanges function exists with OOXML-only parsing. pkg:package wrapper extraction works. w:proofErr normalization applied before parsing. w:ins/w:del/w:moveFrom/w:moveTo extracted with author, date, paragraph context. Adjacent del+ins from same author paired as Replaced via DOM sibling traversal. Table row markers skipped. Run text handles w:t, w:delText, w:br, w:tab, w:cr, w:noBreakHyphen. Namespace-aware querying with fallback. composeSummaryMessages handles {tracked changes} placeholder. All tests pass. Full suite green.</done>
</task>

<task type="auto">
  <name>Task 2: Wire tracked changes Settings UI toggle, WordApi version logging, and summary workflow integration</name>
  <files>src/taskpane/taskpane.html, src/taskpane/taskpane.js, src/taskpane/taskpane.css</files>
  <action>
    **HTML changes (taskpane.html):**

    1. Add tracked changes extraction controls in the Settings section, AFTER the document extraction settings added by plan 04-04 (after the docMaxLength form-group) and BEFORE the Save Settings button. Add:
       ```html
       <div class="form-group checkbox-group">
           <label>
               <input type="checkbox" id="trackedChangesExtraction">
               Include Tracked Changes in Summary
           </label>
           <small class="help-text">Extracts tracked changes (via OOXML) and includes them in the {tracked changes} placeholder</small>
       </div>
       ```

    **CSS changes -- add to taskpane.css (at end of file):**

    2. Add help-text styling if not already present (check first -- it may have been added by plan 04-04):
       ```css
       /* Help text for settings controls */
       .help-text {
           display: block;
           font-size: 11px;
           color: var(--secondary-text);
           margin-top: 4px;
       }
       ```

    **JavaScript changes (taskpane.js):**

    3. Update the import line to add extractTrackedChanges:
       ```javascript
       import { extractAllComments, extractDocumentText, extractDocumentStructured, estimateTokenCount, extractTrackedChanges } from '../lib/comment-extractor.js';
       ```

    4. Add `trackedChangesExtraction` to the config default. After the `docExtraction` property:
       ```javascript
       trackedChangesExtraction: false,
       ```

    5. Update `saveSettings()` to read and save the new field. After the `config.docExtraction = {...}` line (added by plan 04-04):
       ```javascript
       config.trackedChangesExtraction = document.getElementById('trackedChangesExtraction').checked;
       ```

    6. Update `loadSettings()` -- after the `docExtraction` defaults block (added by plan 04-04), add:
       ```javascript
       if (config.trackedChangesExtraction === undefined) {
           config.trackedChangesExtraction = false;
       }
       ```

    7. Update `updateUIFromConfig()` -- after the docExtraction UI updates (added by plan 04-04), add:
       ```javascript
       const trackedChangesCheckbox = document.getElementById('trackedChangesExtraction');
       if (trackedChangesCheckbox) {
           trackedChangesCheckbox.checked = !!config.trackedChangesExtraction;
       }
       ```

    8. Add Word API version detection and logging in `initialize()`, after the existing `supportsComments` detection block (after line ~126 in current code). This is purely diagnostic -- no tier selection:
       ```javascript
       // Detect and log supported Word API version (diagnostics only)
       const apiVersions = ['1.8', '1.7', '1.6', '1.5', '1.4', '1.3', '1.2', '1.1'];
       let detectedVersion = 'unknown';
       try {
           if (typeof Office !== 'undefined' && Office.context && Office.context.requirements) {
               for (const ver of apiVersions) {
                   if (Office.context.requirements.isSetSupported('WordApi', ver)) {
                       detectedVersion = ver;
                       break;
                   }
               }
           }
       } catch (e) { /* detection failed */ }
       addLog(`Word API version: ${detectedVersion}`, 'info');
       ```

    9. Update `handleSummaryGeneration()` to extract tracked changes when the toggle is enabled. After the existing document text extraction block (the `{whole document}` block, after plan 04-04's changes), add:
        ```javascript
        // 3. Extract tracked changes if enabled and summary prompt uses {tracked changes} placeholder
        if (config.trackedChangesExtraction && activeSummaryPrompt && activeSummaryPrompt.template.includes('{tracked changes}')) {
            addLog('Extracting tracked changes (OOXML parsing)...', 'info');
            const tcResult = await extractTrackedChanges();
            addLog(`Tracked changes extracted (${tcResult.changes.length} change(s))`, 'info');

            // Format tracked changes for the prompt -- show before/after with author prominently
            let tcText = '';
            if (tcResult.changes.length > 0) {
                tcText = tcResult.changes.map((c, i) => {
                    const num = i + 1;
                    const author = c.author || 'Unknown';
                    const date = c.date || '';
                    const dateStr = date ? ` on ${date}` : '';

                    if (c.type === 'Replaced') {
                        return `[Change ${num}] REPLACED by ${author}${dateStr}:\n` +
                               `  BEFORE: "${c.beforeText}"\n` +
                               `  AFTER:  "${c.afterText}"` +
                               (c.paragraphText ? `\n  IN CLAUSE: "${c.paragraphText}"` : '');
                    } else if (c.type === 'Deleted') {
                        return `[Change ${num}] DELETED by ${author}${dateStr}:\n` +
                               `  REMOVED: "${c.text}"` +
                               (c.paragraphText ? `\n  IN CLAUSE: "${c.paragraphText}"` : '');
                    } else if (c.type === 'Added') {
                        return `[Change ${num}] ADDED by ${author}${dateStr}:\n` +
                               `  INSERTED: "${c.text}"` +
                               (c.paragraphText ? `\n  IN CLAUSE: "${c.paragraphText}"` : '');
                    } else if (c.type.startsWith('Moved')) {
                        return `[Change ${num}] ${c.type.toUpperCase()} by ${author}${dateStr}:\n` +
                               `  TEXT: "${c.text}"` +
                               (c.paragraphText ? `\n  IN CLAUSE: "${c.paragraphText}"` : '');
                    }
                    return `[Change ${num}] ${c.type} by ${author}${dateStr}: "${c.text}"`;
                }).join('\n\n');
            }

            if (tcText) {
                summaryOpts.trackedChangesText = tcText;
            } else if (tcResult.changes.length === 0) {
                summaryOpts.trackedChangesText = '(No tracked changes found in document)';
            }
        }
        ```

    10. Update the `updateTokenEstimate()` function (added by plan 04-04) to account for tracked changes when the toggle is enabled. Inside the `if (mode === 'summary')` block, after the `{whole document}` token estimation, add:
        ```javascript
        // If prompt uses {tracked changes} and extraction is enabled, add estimate
        if (config.trackedChangesExtraction && summaryPrompt.template.includes('{tracked changes}')) {
            parts.push('+tracked changes');
        }
        ```

    11. Build and verify: `npx webpack --mode development 2>&1 | tail -5`
    12. Run full test suite: `npx jest --no-coverage -x`
  </action>
  <verify>
    <automated>npx webpack --mode development 2>&1 | tail -5 && npx jest --no-coverage -x</automated>
  </verify>
  <done>Settings section shows "Include Tracked Changes in Summary" checkbox with OOXML help text. Word API version detected and logged on initialization (diagnostics only, no tier selection). Tracked changes extraction triggers when toggle is enabled and summary prompt uses {tracked changes} placeholder. Formatted tracked changes text with author prominently displayed is passed to composeSummaryMessages. Config persists via localStorage. Token estimate reflects tracked changes. Webpack builds. All tests pass.</done>
</task>

</tasks>

<verification>
- `npx jest --no-coverage` passes with zero failures (full suite)
- `npx webpack --mode development` builds without errors
- extractTrackedChanges function exported from comment-extractor.js
- OOXML-only: no cascading API detection, no Tier 1 (getTrackedChanges), no Tier 2 (getReviewedText)
- pkg:package wrapper correctly handled (extracts /word/document.xml part)
- w:proofErr elements removed during normalization
- w:ins extracted with author, date, paragraph context
- w:del extracted with author, date, paragraph context from w:delText
- Adjacent w:del + w:ins from same author paired as Replaced via DOM sibling traversal
- w:moveFrom and w:moveTo extracted as separate change types
- Table row revision markers (w:ins/w:del inside w:trPr) skipped
- Text extraction handles w:t, w:delText, w:br, w:tab, w:cr, w:noBreakHyphen
- Namespace-aware querying with getElementsByTagName fallback
- Settings UI shows tracked changes toggle
- Toggle setting persists across reload via localStorage
- When enabled, tracked changes data populates {tracked changes} placeholder in summary prompt
- Author identity prominently displayed in LLM output format (REPLACED by X, DELETED by X, ADDED by X)
- Before/after clearly labeled (BEFORE/AFTER, REMOVED/INSERTED, IN CLAUSE)
- composeSummaryMessages handles {tracked changes} placeholder
- handleSummaryGeneration includes tracked changes in summary workflow
- Token estimate accounts for tracked changes when enabled
- Word API version logged on initialization
</verification>

<success_criteria>
- extractTrackedChanges returns { changes } with array of tracked change objects
- OOXML-only approach: body.getOoxml() + DOMParser, no API version cascading
- pkg:package wrapper extracted to reach /word/document.xml content
- w:proofErr elements normalized (removed) before extraction
- Insertions: { type: 'Added', text, author, date, paragraphText }
- Deletions: { type: 'Deleted', text (from delText), author, date, paragraphText }
- Replacements: adjacent del+ins from same author paired as { type: 'Replaced', beforeText, afterText }
- Moves: w:moveFrom as 'Moved (from)', w:moveTo as 'Moved (to)'
- Table row markers: w:ins/w:del inside w:trPr skipped
- Text extraction: w:t, w:delText, w:br, w:tab, w:cr, w:noBreakHyphen all handled
- Paragraph context: parent w:p text excluding w:del/w:moveFrom containers
- Namespace querying: getElementsByTagNameNS with 'w:*' fallback
- Author identity prominently included in all LLM-formatted output
- LLM sees each change with before/after/clause clearly labeled
- Word API version logged on init (diagnostics)
- OOXML parsing uses browser DOMParser (no new npm dependencies)
- Settings toggle enables/disables tracked changes extraction
- {tracked changes} placeholder in summary prompts replaced with formatted data
- composeSummaryMessages accepts options.trackedChangesText for placeholder replacement
- Tracked changes only extracted when both toggle is enabled AND prompt uses {tracked changes}
- All tests pass, no regressions
</success_criteria>

<output>
After completion, create `.planning/phases/04-document-comment-summary/04-05-SUMMARY.md`
</output>
